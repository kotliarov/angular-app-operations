export class SampleType {
  constructor(
    public id: number,
    public description: string
  ) {
  }
};

export class Status {
  constructor(
    public id: number,
    public description: string
  ) {
  }
};

export class TestType {
  constructor(
    public id: number,
    public description: string
  ) {
  }
};
